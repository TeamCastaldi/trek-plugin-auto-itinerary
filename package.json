{
  "name": "auto-itinerary",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Turns emailed business-trip calendar invites into live TREK trips automatically.",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx trek-plugin-sdk dev",
    "validate": "npm run build && npx trek-plugin-sdk validate",
    "pack": "npm run build && npx trek-plugin-sdk pack",
    "test": "node --test",
    "smoke": "node scripts/smoke-bundle.js",
    "smoke:dev": "node scripts/smoke-dev.js",
    "e2e:mcp": "node scripts/e2e-mcp.js"
  },
  "dependencies": {
    "imap-simple": "^5.1.0",
    "mailparser": "^3.9.0",
    "node-ical": "^0.26.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "trek-plugin-sdk": "^1.4.1"
  }
}
